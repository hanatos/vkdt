set(NAME i-obj)
set(MOD i-obj)
set(SRC_DIR ../../../../src)
set(MOD_DIR ${SRC_DIR}/pipe/modules/${MOD})
set(ASS_DIR ../../assets)
include_directories(${SRC_DIR})
include_directories(${MOD_DIR})

add_library(i-obj SHARED ${MOD_DIR}/main.c)
# target_link_libraries(i-obj z)
