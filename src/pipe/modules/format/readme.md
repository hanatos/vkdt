# format: change pixel format of an image

this can be useful to convert images before writing them out in a specific format.

## connectors

* `input`
* `output`

## parameters

* `channels` the number of channels
* `format` the data type per channel
