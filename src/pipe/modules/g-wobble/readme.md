# g-wobble: animate geometry by wobbling it around

## connectors

* `input` triangle geometry from i-obj for instance
* `output` dynamic triangle geometry, for instance to pass on to bvh

## parameters

* `strength` how much to vary the displacement with the y coordinate of the vertices
* `radius` how far to offset geometry
* `speed` how fast the wobbling will be
